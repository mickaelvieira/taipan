#import "../fragment/bookmark.graphql"
#import "../fragment/event.graphql"

subscription onFavoritesFeedChange {
  favorites {
    ...bookmarkEventFields
    item {
      ...bookmarkFields
    }
  }
}
