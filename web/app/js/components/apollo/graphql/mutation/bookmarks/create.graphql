#import "../../fragment/bookmark.graphql"

mutation create($url: URL!, $isFavorite: Boolean!, $withFeeds: Boolean!) {
  bookmarks {
    create(url: $url, isFavorite: $isFavorite, withFeeds: $withFeeds) {
      ...bookmarkFields
    }
  }
}