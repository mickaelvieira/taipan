#import "../../fragment/source.graphql"

mutation updateTitle($url: URL!, $title: String!) {
  syndication {
    updateTitle(url: $url, title: $title) {
      id
      title
      updatedAt
    }
  }
}