#import "../fragment/user.graphql"

query init {
  users {
    loggedIn {
      ...userFields
      stats {
        bookmarks
        favorites
        readingList
        subscriptions
      }
    }
  }
  app {
    info {
      name
      version
    }
  }
}
