#import "../fragment/user.graphql"

query init {
  users {
    loggedIn {
      ...userFields
      stats {
        ...statsFields
      }
    }
  }
  app {
    info {
      name
      version
    }
  }
}
