#import "../../fragment/source.graphql"
#import "../../fragment/log.graphql"

query source($url: URL!) {
  syndication {
    source(url: $url) {
      ...sourceFields
      logEntries {
        ...logFields
      }
    }
  }
}
