#import "../fragment/document.graphql"

query News($from: String, $to: String, $limit: Int) {
  News(from: $from, to: $to, limit: $limit) @connection(key: "feed_news") {
    total
    first
    last
    limit
    results {
      ...documentFields
    }
  }
}
