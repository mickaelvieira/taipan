#import "../fragment/bookmark.graphql"

query GetLatestBookmarks($offset: Int, $limit: Int) {
  GetLatestBookmarks(offset: $offset, limit: $limit) {
    total
    offset
    limit
    results {
      ...bookmarkFields
    }
  }
}
