#import "../fragment/bookmark.graphql"

query ReadingList($pagination: CursorPaginationInput!) {
  GetReadingList(pagination: $pagination) @connection(key: "feed_reading_list") {
    total
    first
    last
    limit
    results {
      ...bookmarkFields
    }
  }
}
