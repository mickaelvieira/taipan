#import "../fragment/bookmark.graphql"

query Favorites($from: String, $to: String, $limit: Int) {
  GetFavorites(from: $from, to: $to, limit: $limit) @connection(key: "feed_favorites") {
    total
    first
    last
    limit
    results {
      ...bookmarkFields
    }
  }
}
