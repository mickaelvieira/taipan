#import "../fragment/bookmark.graphql"

query Favorites($pagination: CursorPaginationInput!) {
  GetFavorites(pagination: $pagination) @connection(key: "feed_favorites") {
    total
    first
    last
    limit
    results {
      ...bookmarkFields
    }
  }
}
