#import "../fragment/bookmark.graphql"

mutation ChangeBookmarkReadStatus($url: String!, $isFavorite: Boolean!) {
  ChangeBookmarkReadStatus(url: $url, isRead: $isFavorite) {
    ...bookmarkFields
  }
}